# Stage 8 LFTNet eval config — placeholder until official complete release is available.
release_root: null  # fill after placing full Zenodo/GitHub tree
instance_root: data/INSTANCE  # symlink; use realpath in scripts; never copy HDF5
threshold_grid: [0.05, 0.1, 0.2, 0.3, 0.5, 0.7]
sota_claim_allowed: false
post_confirm_external_comparator: true
reuse_stage6_main_method: fixed_rescore_UNION
forbid_train: true
forbid_confirm_tuning: true
